<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.javaee_ecomorder.mapper.AuditLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_log (user_id, username, operation_type, content, result, create_time)
        VALUES (#{userId}, #{username}, #{operationType}, #{content}, #{result}, #{createTime})
    </insert>

</mapper>
