server:
  port: 8082
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecom_order?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 18092786035
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      database: 1

jwt:
  secret: JavaEE-Ecom-Order-Secret-Key-For-JWT-Signing-2024
  expiration: 3600000

security:
  login-failure-max-attempts: 5
  login-failure-lock-duration: 180
  cors-allowed-origins: http://localhost:8081,http://localhost:5173

ecom:
  aop:
    performance-threshold-ms: 500
    audit-log-enabled: true
    cache-enabled: true

logging:
  level:
    com.example.javaee_ecomorder: debug
